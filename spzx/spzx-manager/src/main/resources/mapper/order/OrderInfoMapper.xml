<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.manager.mapper.OrderInfoMapper">

    <select id="orderTotalAmountStastistics" resultType="com.model.entity.order.OrderStatistics">
        select sum(total_amount)                    as totalAmount,
               sum(id)                              as totalNum,
               date_format(create_time, '%Y-%m-%d') as orderDate
        from order_info
#         where date_format(create_time, '%Y-%m-%d') = #{createTime}
        group by date_format(create_time, '%Y-%m-%d')
    </select>
</mapper>